# AVPlayerCaching
This project doesn't work because implementation via AVResourceLoader is bad solution for me.
I am trying now to implement custom caching of resources for AVPlayer with local HTTP server (you can take a look at https://github.com/swisspol/GCDWebServer).
